# =====================================================================
# FAKE TEST VALUES - DO NOT TREAT AS REAL CREDENTIALS
# =====================================================================
# Every value below is synthetic. The literal substring FAKETESTVALUE is
# embedded in each so a scanner or human reviewer can confirm at a glance.
# This file exists to be discovered by TruffleHog as part of Emulation 1
# (T1552.001 Unsecured Credentials in Files) for the UNC6395 / Salesloft
# Drift hunt package. See ../../README.md.
# =====================================================================

salesforce:
  # Drift OAuth client integration. Refresh token is long-lived and would,
  # if real, allow minting access tokens against Salesforce REST API
  # (T1528 Steal Application Access Token, T1550.001 Use Alternate Auth
  # Material) without re-prompting the user for MFA.
  client_id: "3MVG9FAKETESTVALUE_CLIENT_ID_PLACEHOLDER_AAAAAAAAAAAAAAAAAA"
  client_secret: "FAKETESTVALUE_CLIENT_SECRET_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
  # Salesforce refresh tokens typically begin "5Aep" and are ~80 chars.
  # The string below mimics that shape but is FAKE.
  refresh_token: "5Aep861FAKETESTVALUEqVZxJ9Hk7nM2pQ4tR6sU8vW0xY1zA3bC5dE7fG9h"
  instance_url: "https://example-dev-ed.develop.my.salesforce.com"
  api_version: "v58.0"

aws:
  # Used by the worker to read additional service tokens out of SSM.
  # NOTE: TruffleHog's AWS detector did NOT fire on the captured run
  # (the FAKETESTVALUE substring breaks the AKIA[A-Z0-9]{16} regex).
  # Left in place as a fixture for future detector tuning; not a captured
  # finding. See ../output/truffle.summary.txt.
  access_key_id: "AKIAFAKETESTVALUE001"
  secret_access_key: "FAKETESTVALUEFAKETESTVALUEFAKETESTVALUEz"
  region: "us-east-1"
  ssm_parameter_prefix: "/drift-sync/prod/"

snowflake:
  # Mining stolen Salesforce data for Snowflake credentials was part of
  # UNC6395's secondary objective. We plant one here for completeness.
  account: "fakeacct-fa12345.snowflakecomputing.com"
  user: "DRIFT_SYNC_SVC"
  password: "FAKETESTVALUE_Sn0wPw_PlaceholderDoNotUse!"
  warehouse: "DRIFT_WH"
  database: "DRIFT_RAW"

# Sentry DSN. NOTE: TruffleHog's Sentry detector did NOT fire on the
# captured run; left in place as a fixture, not a captured finding.
sentry_dsn: "https://FAKETESTVALUE0123456789abcdef@o0.ingest.sentry.io/000000"

# Slack webhook for ops alerts. TruffleHog's SlackWebhook detector
# is regex-only and DID fire on this URL shape during the captured
# scan (one of the two detectors that produced findings, alongside
# PrivateKey on signing_key.pem).
# THIS IS A FAKE TEST VALUE - this webhook does not exist.
slack_alert_webhook: "https://hooks.slack.com/services/T0FAKETESTVAL/B0FAKETESTVAL/aAbBcCdDeEfFgGhHiIjJkKlL"

# Session signing token used by the worker for downstream service auth.
# JWT shape (eyJ.<payload>.<signature>). NOTE: TruffleHog's JWT
# detector did NOT fire on this run; left in place as a fixture.
# This token is FAKE - the signature does not validate against any key.
session_jwt: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJGQUtFVEVTVFZBTFVFIiwibmFtZSI6IkRyaWZ0IFN5bmMgU2VydmljZSIsImlhdCI6MTUxNjIzOTAyMn0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"

# Service-account RSA private key path. The actual key bytes live in
# config/signing_key.pem so they sit at column zero (TruffleHog's
# PrivateKey detector calls pem.Decode, which expects markers on
# their own lines without indentation).
signing_key_path: "./config/signing_key.pem"
